import {View, Text} from 'react-native';
import {SCREEN_HEIGHT, SCREEN_WIDTH, _MPY_} from '../utils/const';

const Home = () => {
  // js code

  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
